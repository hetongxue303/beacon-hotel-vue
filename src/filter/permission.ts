export const filterMenu = (menus: Array<any>, parentId: number) => {
    const data: Array<any> = []
    // TODO 具体过滤菜单操作
    return data
}
export const filterRouter = (menus: Array<any>, parentId: number) => {
    const data: Array<any> = []
    // TODO 具体过滤路由操作
    return data
}